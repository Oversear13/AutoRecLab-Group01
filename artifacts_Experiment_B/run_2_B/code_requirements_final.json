["Use OmniRec as the only experiment orchestration layer; do not invoke LensKit, RecBole, or other recommender backends directly outside OmniRec wrappers.", "Load the MovieLens1M dataset via OmniRec\u2019s registered dataset identifier for MovieLens1M.", "Convert the ratings to implicit feedback before training so the task is top-N recommendation.", "Use a reproducible user-level holdout split for train/validation/test with a fixed random seed.", "Train exactly two recommendation algorithms in the experiment: one of them must be an implicit-feedback model and both must be run through OmniRec.", "Evaluate both algorithms on the held-out test set with ranking metrics NDCG@10 and Recall@1.", "Keep the experiment in a single runnable OmniRec flow that handles preprocessing, training, prediction, and evaluation end-to-end.", "Return a concise comparison summary that reports both algorithms\u2019 NDCG@10 and Recall@1 values in a directly comparable format.", "If the prototype already generates plots, add only a compact comparison plot for the two algorithms over NDCG@10 and Recall@1; otherwise do not introduce plotting solely for its own sake."]