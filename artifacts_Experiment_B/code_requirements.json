["Use OmniRec for the full experiment pipeline, including dataset loading, preprocessing, training, and evaluation.", "Load the MovieLens 100K 'u.data' source via OmniRec's registered MovieLens100K dataset loader.", "Convert explicit ratings to implicit feedback with a single declared relevance threshold before top-N evaluation.", "Use one deterministic train/validation/test split strategy suitable for top-N recommendation.", "Fix and report the random seed used for splitting and model training.", "Compare exactly two algorithms under the same preprocessing and evaluation protocol.", "Configure both algorithms within the same experiment plan and evaluation run.", "Evaluate the held-out test set with ranking metrics NDCG@10 and Recall@1.", "Report the two algorithms' metric values in a concise results table.", "Record the dataset loader/version, split configuration, algorithm names, and metric cutoffs so the experiment can be rerun exactly."]