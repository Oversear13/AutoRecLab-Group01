["Use OmniRec as the only recommender-system framework for data loading, preprocessing, model training, and evaluation.", "Load the MovieLens 1M dataset from OmniRec's registered dataset loader, using the MovieLens ratings file as the source.", "Convert explicit ratings to implicit feedback before training and evaluation, because the task is top-N recommendation with ranking metrics.", "Use a fixed, reproducible user-level holdout split that is shared by both algorithms.", "Compare exactly two algorithms: one item-neighborhood method and one implicit-feedback ranking method available in OmniRec.", "Evaluate both algorithms on the same test split with NDCG@10 and Recall@1.", "Set all available random seeds or stochastic controls exposed by OmniRec to make the run reproducible.", "Preserve the existing script structure and extend it incrementally rather than rewriting the pipeline from scratch.", "Print or save experiment metadata, including dataset name, split protocol, algorithm names, and metric cutoffs.", "Generate a comparison plot for the two algorithms on the requested metrics.", "Run the full end-to-end workflow in OmniRec: load, preprocess, train, evaluate, and report results for MovieLens 1M only."]