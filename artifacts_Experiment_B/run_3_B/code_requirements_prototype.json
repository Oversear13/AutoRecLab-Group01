["Use exactly one dataset: MovieLens100K via OmniRec's registered dataset loader; do not reference additional datasets.", "Use exactly one algorithm in the prototype; choose a simple, fast OmniRec-supported algorithm and do not compare against a second model.", "Set OmniRec's random state before loading, preprocessing, training, or splitting to make the pilot reproducible.", "Convert the ratings data to implicit feedback only if required by the chosen algorithm; otherwise keep preprocessing minimal.", "Use one train/validation/test user holdout split in the preprocessing pipeline.", "Run the experiment end-to-end through OmniRec's experiment runner rather than manual backend calls.", "Evaluate with a minimal ranking metric set that includes NDCG@10 and Recall@1 only.", "Do not add extra metrics, extra cutoffs, or hyperparameter sweeps in the prototype stage.", "Produce at least one plot from the resulting experiment outputs to validate the reporting pipeline.", "Return dataset statistics after preprocessing and the metric values at the requested cutoff(s)."]