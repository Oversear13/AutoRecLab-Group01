["Use OmniRec as the only orchestration layer for dataset loading, preprocessing, splitting, training, and evaluation.", "Load MovieLens100K and treat ratings greater than 3 as implicit positive interactions before filtering or splitting.", "Apply 5-core filtering to the implicit interaction data before generating train/validation/test splits.", "Run exactly three algorithms: ALS, ItemKNN, and Pop.", "Use five distinct, seed-controlled random split seeds.", "For each seed, create one random holdout split with proportions 0.70 train, 0.15 validation, and 0.15 test.", "Evaluate every algorithm on the same split for a given seed.", "Use a single, consistent evaluation metric set across all algorithms and seeds.", "Train models only on the training portion and use validation only for model selection if the implementation requires it.", "Report per-seed results and aggregated results across seeds to quantify sensitivity to split randomness.", "Record the preprocessing, split proportions, and seeds used so the experiment is reproducible."]